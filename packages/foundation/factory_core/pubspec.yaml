name: factory_core
description: Core utilities — Failure type, fpdart re-exports, and common typedefs.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  fpdart: ^1.1.0
