name: factory_ui
description: Design tokens, theme, and base widgets. Zero business logic dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.8.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
