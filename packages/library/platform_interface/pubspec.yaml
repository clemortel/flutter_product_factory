name: platform_interface
description: Abstract interfaces for platform services (API, Auth, Analytics, Storage).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  core:
