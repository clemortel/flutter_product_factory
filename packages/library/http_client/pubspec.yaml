name: http_client
description: HTTP-based ApiClient implementation and in-memory fake.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  core:
  platform_interface:
  dio: ^5.9.0
  talker_dio_logger: ^4.5.0
